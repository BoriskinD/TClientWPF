<Window x:Class="TClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TClient" Height="471" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel  Grid.Row="0" Orientation="Vertical">
            <Button Command="{Binding SettingsCommand}" 
                    Content="Settings"
                    Width="200"
                    Margin="296,15,296,20"/>
            <Button Content="Start"
                    Width="200"
                    Command="{Binding StartCommand}"
                    Margin="296,1,296,20"/>
            <TextBox Height="293"
                     Name="loger"
                     IsReadOnly="True"
                     Text="{Binding GetLog, Mode=OneWay}"/>
            <StatusBar Height="25" Margin="0,5,0,0">
                <StatusBarItem>
                    <TextBlock Text="Status:"/>
                </StatusBarItem>
                <StatusBarItem>
                    <Image Source="Images/Online.png"
                           Width="30"
                           Height="25"/>
                </StatusBarItem>
                <Separator/>
                <StatusBarItem>
                    <TextBlock Text="Number of forwarded messages:"/>
                </StatusBarItem>
                <StatusBarItem>
                    <TextBlock Text="0"/>
                </StatusBarItem>
            </StatusBar>
        </StackPanel>
    </Grid>
</Window>
